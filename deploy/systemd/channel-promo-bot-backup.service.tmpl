[Unit]
Description=Backup channel-promo-bot SQLite DB

[Service]
Type=oneshot
User={{RUN_USER}}
WorkingDirectory={{APP_DIR}}
ExecStart=/bin/bash -lc 'cd "{{APP_DIR}}" && VERIFY_BACKUP=1 ./scripts/backup_db.sh'
