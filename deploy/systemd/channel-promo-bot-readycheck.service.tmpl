[Unit]
Description=Ready check for channel-promo-bot

[Service]
Type=oneshot
User={{RUN_USER}}
WorkingDirectory={{APP_DIR}}
ExecStart=/bin/bash -lc 'cd "{{APP_DIR}}" && ./scripts/healthcheck.sh || ./scripts/alert_admin.sh "❌ {{SERVICE_NAME}} /ready 失败"'
